systems (like programming language tokens) can't support arbitrary qualia
mapping because there's nothing to evaluate against. But your tokens decode to
physical trajectories that the LLM can see and judge using its existing world
knowledge. The simulator isn't just a verification tool -- it's the perceptual
system that lets the LLM experience what the tokens mean. And once the LLM can
experience token meanings, it can map them to any English concept it already
understands, not just formal physics terms.

